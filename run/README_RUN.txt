This is the working directory while running/ debugging from the Visual Studio IDE.

If running the test executable from the Windows terminal, then the ..\ext_dlls\ folder must be added to the PATH environmental variable. Otherwise the dlls will fail to load.
